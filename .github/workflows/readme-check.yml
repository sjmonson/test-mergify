name: README Check

on:
  pull_request:

jobs:
  check-indicator:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Check for ci:ready marker
        run: grep -qF '<!-- ci:ready -->' README.md
